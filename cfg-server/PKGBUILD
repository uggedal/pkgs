# Maintainer: Eivind Uggedal <eivind@uggedal.com>
pkgname=cfg-server
pkgver=0.0.2
pkgrel=1
pkgdesc="Server configuration"
arch=(any)
url="https://uggedal.com"
license=(Unlicense)
depends=(
	openssh
)
install=cfg-server.install

rootdir="$PWD"

package() {
	install -D -m644 "$rootdir/sshd_config" "$pkgdir/etc/ssh/sshd_config"
}
